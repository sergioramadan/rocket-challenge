import Modal from '../Modal';
import './Loader.css';

const Loader = () => {
    return (
        <Modal>
            <div className="loader">
                <p>Loading...</p>
            </div>
        </Modal>
    )
};

export default Loader;
